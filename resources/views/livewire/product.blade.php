{{-- <div class="col-md-4">
    <div>
        <span >Категории Бренды:</span>
        <i data-feather="loader" style="height: 100px; width: 100px" ></i>
        <select class="" id="brend_id" class="calc__type" name="brend_id"  id="calc__type" style="width: 100%; padding:6px 12px; border-color: #ced4da; border-radius: 5px " >
            @foreach ($brends as $brend)
                <option value="{{ $brend->id }}" >{{ $brend->name }}</option>
            @endforeach
        </select>
    </div>
</div>
<div class="col-md-4">
    <div>
        <span >Категории Категории:</span>
        <i data-feather="loader" style="height: 100px; width: 100px" ></i>
        <select class="" id="brend_id" class="calc__type" name="brend_id"  id="calc__type" style="width: 100%; padding:6px 12px; border-color: #ced4da; border-radius: 5px " >
            @foreach ($brends as $brend)
                <option value="{{ $brend->id }}" >{{ $brend->name }}</option>
            @endforeach
        </select>
    </div>
</div> --}}